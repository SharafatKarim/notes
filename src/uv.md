# UV

The python package `uv` provides a simple interface for working with UV coordinates in 3D graphics. UV coordinates are used to map textures onto 3D models, allowing for detailed surface appearances.

## Gobal package

```bash
uv tool install git-filter-repo
```