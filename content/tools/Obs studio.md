## Intel Hardware Acceleration 
1. Install the Intel VPL GPU runtime:  
  ```
  sudo pacman -S vpl-gpu-rt  
  ```
  
> Optionally, install libva-utils if you want the vainfo tool for inspecting hardware codec support:  
>
> ```
sudo pacman -S libva-utils
>```
